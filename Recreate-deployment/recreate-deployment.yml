apiVersion: apps/v1
kind: Deployment
metadata:
  name: online_shop
  namespace: recreate-ns
  labels:
    app: online_shop
spec:
  replicas: 4
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: online_shop
  template:
    metadata:
      labels:
        app: online_shop
    spec:
      containers:
      - image: amitabhdevops/online_shop
        name: online_shop
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
